# ----------
# 0. ALIASES
# ----------
alias ls='lsd -l'
alias la='lsd -la'
alias lt='lsd --tree'

alias vi='nvim'
alias ff='fastfetch'

alias sf='spf'
alias cat='bat'

alias grep='grep --color=always'